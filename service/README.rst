Github Metrics
##############

Run code locally

.. code-block:: bash

    $ redis_docker
    $ . env/bin/activate
    $ pip install bert-etl -U
    $ AWS_PROFILE=stsci bert-runner.py -m collectMetrics -f

